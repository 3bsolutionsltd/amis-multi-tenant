# Tooling (Confirmed Defaults)

## Dev
- Node.js LTS
- pnpm
- Docker + docker compose
- Postgres + Redis via compose
- dbmate for migrations (SQL-first)
- Fastify + TypeScript for API
- React + TS + Vite for web

## Standard commands (target)
- docker compose up -d
- pnpm install
- dbmate up
- pnpm dev
- pnpm test