# Copilot Instructions — AMIS Platform (v3)

Follow the rules in `.copilot-context/03-COPILOT-INSTRUCTIONS.md`.

Confirmed defaults:
- Monorepo + pnpm workspaces
- Fastify + TypeScript (API)
- PostgreSQL + dbmate SQL migrations + RLS
- React + TS + Vite (Web)
- Zod for validation